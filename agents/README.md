# Agent prompts

These prompts are meant for other AI coding/design agents to implement Promptly based on the docs in `docs/`.

Order of operations:
1) Answer decisions in `docs/01-discovery.md`
2) Run `agents/01-ux-agent.md`
3) Run `agents/02-rule-engine-agent.md`
4) Run `agents/03-llm-rubric-agent.md` (if using LLM rewrites)
5) Run `agents/04-fullstack-agent.md` to build the product

